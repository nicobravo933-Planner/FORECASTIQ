# deploy.yml — DESHABILITADO hasta Phase 6
#
# Railway deploya directo desde el repo GitHub — no necesita este workflow.
# Configuración en Phase 6:
#   1. railway.com → New Project → Deploy from GitHub repo
#   2. Apuntar a /backend, configurar env vars
#   3. Vercel → Import Git Repository → apuntar a /frontend
#   4. Reactivar este archivo si se necesita lógica extra post-deploy
#
# Por ahora el CI (ci.yml) es suficiente: lint + tests en cada push.

name: Deploy

on:
  workflow_dispatch:   # solo se activa manualmente, nunca automático

jobs:
  placeholder:
    name: Deploy placeholder — configure in Phase 6
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy will be configured in Phase 6 (Railway + Vercel)"
