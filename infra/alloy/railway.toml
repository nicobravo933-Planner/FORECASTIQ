# railway.toml — Grafana Alloy service
# Alloy no tiene healthcheck HTTP estándar — Railway lo gestiona por proceso

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
