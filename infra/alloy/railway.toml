# railway.toml — Grafana Alloy service
# Scrapes /metrics from forecastiq-api and forwards to Grafana Cloud

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/-/healthy"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
